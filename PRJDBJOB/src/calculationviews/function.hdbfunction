FUNCTION "TEST_FUNCTION" ( )
      RETURNS Table(
     "MANDT" NVARCHAR(3),
	 "CARRID" NVARCHAR(3),
	 "CARRNAME" NVARCHAR(20),
	 "CURRCODE" NVARCHAR(5),
	 "URL" NVARCHAR(255)
      )
      LANGUAGE SQLSCRIPT
    --  SCHEMA NEW_PROJ_SB_HDI_DB
      SQL SECURITY INVOKER AS
      
BEGIN

RETURN

		Select * from SCARR_DAP;

END;